{
  "name": "grok-cookie-refresh",
  "version": "1.0.0",
  "description": "Auto-refresh grok.com cookies for grok2api using Playwright persistent context",
  "scripts": {
    "login": "node cookie_refresh.js --login",
    "refresh": "node cookie_refresh.js --refresh",
    "status": "node cookie_refresh.js --status"
  },
  "dependencies": {
    "playwright": "^1.52.0",
    "ws": "^8.21.0"
  }
}
